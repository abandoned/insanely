- form_for([@project, @task, setup_assets(@comment)], :html => { :multipart => true }) do |f|
  = f.error_messages
  %ul
    %li
      = f.text_area :message, :rows => 5
    = render :partial => 'assets/form', :locals => { :form => f }
    - if @project.participants.count > 1
      %li
        = check_box_tag :notify
        %span.small Notify others by email
    %li
      = f.submit('Leave comment »', :class => 'no-margin button')
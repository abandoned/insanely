- title 'Workmates'

-content_for :aside do
  = render :partial => 'shared/resource_states', :locals => { :links => project_links}

.actions
  = link_to('Invite a person', new_workmate_path)

-if @workmates.count > 0
  %ul#workmates= render :partial => 'workmate', :collection => @workmates
- else
  It's all tabula rasa here.
      
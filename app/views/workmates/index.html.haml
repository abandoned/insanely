- title 'Workmates'

-content_for :aside do
  = render :partial => 'shared/resource_states', :locals => { :links => project_links}

#actions
  = link_to('Add a person', new_workmate_path)

-if @collaborations.count > 0
  %ul#workmates
    = render :partial => 'collaboration', :collection => @collaborations
- else
  It's all tabula rasa here.
      
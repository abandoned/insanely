- title 'Add a workmate'

-content_for :aside do
  = render :partial => 'shared/resource_states', :locals => { :links => project_links}

- form_for(:workmate, @workmate, :url => workmates_path, :html => { :class => 'dressed-up'} ) do |f|
  = f.error_messages
  %dl
    %dt
      = f.label :login, 'Login of workmate'
    %dd
      = f.text_field :login
    %dd
      = f.submit 'Add workmate »', :class => 'button'
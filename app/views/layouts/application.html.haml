!!!
%html
  %head
    %title= yield(:title)
    = render :partial => 'layouts/head'
  %body.regular
    #header
      .wrapper
        -project_title
        - if in_project
          - if @project.description.present?
            = render 'projects/blurb'
          = render 'layouts/navigation'
        - if current_user
          = render 'layouts/user_nav'
    #page.clearfix
      #content
        - flash_message
        = yield
      - if @content_for_aside
        #aside
          = yield(:aside)
    #footer
      .wrapper
        %p
          A
          = link_to('copylefted', 'http://github.com/snl/insanely')
          byproduct of
          = link_to('Paper Cavalier', 'http://papercavalier.com')
        